# Sample submissions for HemoSight Audit

Generated by `scripts/make_sample_submissions_phase6.py`. Every file here is **synthetic** - a seeded RNG, no real measurement of anything - which is why it can be tracked in git.

Two further submissions, derived from this project's REAL data and carrying verdicts already on the record in CLAUDE.md, are written to `data/interim/phase6/known_truth/` instead. They hold venous-blood haemoglobin and PPG features for 252 real subjects; eight of ten datasets in this project ship no licence file, so dataset-derived measurements are not committed. Run the script to regenerate them.

## Images

The duplicate check needs pixels. sample_images.zip and the loose PNGs live in `data/interim/phase6/` (binary artefacts are never tracked). On the upload screen either point the directory field at:

```
C:\Users\demia\Desktop\HemoSight\data\interim\phase6\sample_images
```

or upload the archive `C:\Users\demia\Desktop\HemoSight\data\interim\phase6\sample_images.zip`.

## Files

| file | rows | what it contains | checks that do not pass |
| --- | --- | --- | --- |
| `01_mixed_start_here.csv` | 248 | Several faults at once, and everything the optional checks need. The one to upload first. | `duplicates` fail, `split_integrity` fail |
| `02_clean_no_injected_fault.csv` | 240 | No fault. Every check should pass or decline; any FAIL here is a false positive. | none - all eight pass |
| `03_fault_duplicates_across_split.csv` | 243 | byte-identical images on both sides of the split | `duplicates` fail, `split_integrity` fail |
| `04_fault_subject_in_train_and_test.csv` | 245 | the same subject_id in train and test | `duplicates` fail, `split_integrity` fail |
| `05_fault_model_is_a_sex_classifier.csv` | 240 | a model that is a sex classifier with a decoration | `demographic_baseline` fail, `proxy_probe` fail |
| `06_fault_model_has_no_signal.csv` | 240 | a model with no association to the target | `demographic_baseline` fail, `permutation` fail, `seed_stability` fail, `subgroup_robustness` fail, `proxy_probe` insufficient data |
| `07_fault_score_moves_with_the_seed.csv` | 240 | a model whose score moves with the seed by more than its effect | `seed_stability` fail |
| `08_fault_effect_carried_by_8pct_of_subjects.csv` | 240 | an advantage carried by 8% of subjects | `demographic_baseline` fail, `subgroup_robustness` fail |
| `09_control_genuine_signal.csv` | 240 | A real effect. The permutation test must NOT call this out. | `demographic_baseline` fail |
| `data/interim/phase6/known_truth/phase1_ghana_pool_overlap.csv` | 9232 | Phase 1: 419 shared MD5 hashes, 1,708 ids -> 1,067 leak-proof groups. image_path points into data/raw, which is READ-ONLY; upload with no image directory and the paths resolve as absolute. | see CLAUDE.md |
| `data/interim/phase6/known_truth/phase4_ppg_features_only.csv` | 252 | Phase 4 Gate B: MAE 1.190 g/dL against sex alone at 0.831. | see CLAUDE.md |
| `data/interim/phase6/known_truth/phase4_ppg_plus_demographics.csv` | 252 | Phase 4 Task 3: MAE 0.824 g/dL, inside the pre-declared VIABLE band, and a sex classifier. The proxy probe is the check that says so. | see CLAUDE.md |

Those verdicts were MEASURED by running the harness over each file, not predicted. They assume the image directory above is supplied; without it the duplicate check returns INSUFFICIENT DATA rather than a verdict.

Three things in that table are worth reading carefully rather than treating as noise:

- **A fault often trips more than one check, and that is correct.** Byte-identical images copied across the split (file 03) break the content axis of split integrity as well, because the leak-proof grouping joins subject ids to content hashes. A subject duplicated across the split (file 04) points at the same image file twice, so the duplicate check sees it too. Neither is a spurious hit; they are the same fault seen from two directions.
- **File 09 carries a real effect and still fails the demographic baseline.** On this seed, sex alone edges past the model. That is the borderline behaviour measured in `reports/phase6_audit_harness.md` section 7.3: checks 3 and 7 compare point estimates with no uncertainty interval, so near the boundary the verdict moves with the sample. The check says so in its own explanation when the margin is narrow.
- **File 06 returns INSUFFICIENT DATA from the proxy probe.** A model with no signal has no apparent skill for a demographic variable to account for, so the question has no answer rather than a negative one. That is the third verdict doing its job.
