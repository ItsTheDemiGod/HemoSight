# Content review - plain and technical layers, side by side

Generated by `scripts/phase8a_content_review.py` from `hemosight.audit.content`. The rule under review: **simplify the sentence, never the claim.** For each check the plain layer is shown beside the technical layer; a reviewer should look for any plain sentence that claims more or less certainty than its technical counterpart.

## 1. Static content per check

### Duplicate images across the split  (`duplicates`)

*Question:* Are the same pictures in both the training set and the test set?

| layer | text |
| --- | --- |
| what it means (plain) | A model tested on pictures it was trained on gets credit for remembering, not for measuring. Duplicates are common in medical image collections - the same photograph filed under two names, or two datasets distributed separately that share files - and they are invisible in a results table. |
| mechanism (plain) | When a copy of a training image sits in the test set, the model has already seen the answer. The score rises for reasons that have nothing to do with the body. This project found 419 identical images shared between two datasets that were published as independent sources; one was 87% contained inside the other. |
| mechanism (technical) | MD5 over file bytes for exact duplicates; pHash and dHash with a Hamming threshold for near-duplicates; connected components over the near-duplicate graph; an ImageNet-embedding nearest-neighbour pass for semantic near-copies. The decisive statistic is the number of duplicate components whose members carry more than one split label. Duplicate rate alone is reported but does not fail the check unless a split is supplied and crossed. |
| provenance | Phase 1 overlap check: CP-AnemiC and the Ghana conjunctiva set shared 419 MD5 hashes and 98.2% of one fell within pHash 10 of the other; they were merged into one site. Two other collections were 52.7% and 50.8% internally redundant. |
| glossary terms | content hash, split, near-duplicate |

**Headlines by verdict, with representative measured values:**

| verdict | plain headline | what to do |
| --- | --- | --- |
| FAIL | 4 groups of identical or near-identical images sit on both sides of the train/test split, so the model was tested on pictures it had already seen. | **FIXABLE** - Remove duplicates by content hash BEFORE splitting, and rebuild the split so that every copy of an image lands on the same side. Then re-run the model. The score will usually drop; the new score is the real one. |
| PASS | No duplicate images were found among the 100 supplied. | **REPORT IT** - Nothing to change. State in the write-up that duplicates were checked by content hash and none cross the split; readers cannot tell that from a score. |
| INSUFFICIENT DATA | We could not check this - your file did not include a split column saying which rows were used for training and which for testing. | **FIXABLE** - Supply the images (a folder or an image_path column), and run again. Until then this check has no verdict, and the report says so; it must not be read as a pass. |

### People kept apart across the split  (`split_integrity`)

*Question:* Was the model tested on people it never saw during training?

| layer | text |
| --- | --- |
| what it means (plain) | If one person's images are split between training and test, the model can recognise the person rather than the condition. Splitting by row or by image instead of by person is the single most common way a medical model's score is inflated, and it cannot be seen in the score. |
| mechanism (plain) | Subjects contribute several images, and identical images sometimes appear under different subject ids. A split that keeps subject ids apart can still put the same picture on both sides. This project's 1,708 nominal subject ids collapsed to 1,067 genuinely separate groups once identical content was linked; the difference is exactly what a plain subject split would have leaked. |
| mechanism (technical) | Rows are grouped by connected components of a graph joining subject ids and content hashes (MD5 of the image). The check counts subject ids present in more than one split label, counts nominal ids against leak-proof components, and - if the submitter declared a grouping column - counts declared groups that cut through a component. Without images the content axis cannot be checked and the check declines on that axis rather than passing on the subject axis alone. |
| provenance | Phase 1 splits: grouping by (subject_id, content hash) components collapsed 1,708 ids to 1,067 groups in the Ghana pool. An automated leak check fails the split script on any violation. |
| glossary terms | split, content hash, connected components, leakage |

**Headlines by verdict, with representative measured values:**

| verdict | plain headline | what to do |
| --- | --- | --- |
| FAIL | 4 subjects have data in more than one part of the split, so the model was trained and tested on the same people; 50 subject ids collapse to 40 genuinely separate people or images once identical content is linked, so the split leaks even where the ids look clean. | **FIXABLE** - Split by connected components of (subject id, content hash) - every record that shares a person OR a pixel-identical image goes to the same side - not by subject id alone. Freeze the split with a seed and record it. This project's own splits collapsed 1,708 nominal ids into 1,067 leak-proof groups; the gap was the leak. |
| PASS | Every one of the 50 subjects is on one side of the split only, and no identical content links records across sides. | **REPORT IT** - Nothing to change. Record how the split was grouped and the seed, so a reader can see that the model never met its test subjects. |
| INSUFFICIENT DATA | We could not check this - your file did not include a split column saying which rows were used for training and which for testing. | **FIXABLE** - Supply a split column (train / calibration / test per row) and, if the check asked for them, the images, and run again. Until then this check has no verdict, and the report says so; it must not be read as a pass. |

### Beating the obvious guess  (`demographic_baseline`)

*Question:* Does the model do better than simply knowing the subject's sex, age, device or site?

| layer | text |
| --- | --- |
| what it means (plain) | A screening model is only useful if it knows something a form would not have told you. Sex, age and where the data came from often predict the outcome surprisingly well on their own, and a model can learn to read them from the data. A score reported without this comparison cannot be interpreted. |
| mechanism (plain) | Men naturally carry more haemoglobin than women - the WHO anaemia thresholds themselves differ by sex - so a model that has effectively learned to detect sex will look like it detects blood. This project measured exactly that in Phase 4: sex alone predicted haemoglobin with an error of 0.831 g/dL, better than every model built across two modalities. In Phase 6.5 the same thing happened with the collection site: two sites differed by 2.4 g/dL in mean haemoglobin, and an image model 'beat demographics' until site was added to them. |
| mechanism (technical) | On identical whole-subject folds: the population mean, each submitted demographic alone (ridge), and all together, against the submitted predictions. The verdict compares the model's MAE with the best single-variable baseline; a margin below 5% of the target's SD is flagged as narrow. Every non-signal variable the model could have learned belongs in the baseline - site and device as much as sex and age. |
| provenance | Phase 4 Task 3: sex alone 0.831 g/dL vs four-wavelength PPG 1.190; 'PPG + demographics' 0.824 was a sex classifier. Phase 6.5: site + sex + age 1.273 vs image CNN 1.301 on Eyes-Defy, after a first run that omitted site. |
| glossary terms | baseline, MAE, identical folds |

**Headlines by verdict, with representative measured values:**

| verdict | plain headline | what to do |
| --- | --- | --- |
| FAIL | Knowing only the subject's sex predicts the outcome as well as or better than the model (error 0.83 g/dL versus the model's 1.19 g/dL). | **REPORT IT** - No code change makes a model that loses to a demographic variable measure what it claims. Report the model beside the baseline on identical folds, and say which one wins. This project reported sex alone at 0.831 g/dL beating every model it built, and closed the arm. |
| PASS | The model beats the best single demographic guess (sex, 0.81 g/dL) with an error of 0.65 g/dL. The margin is small (+0.020 g/dL) compared with how much the outcome varies, so this verdict could flip with a different sample. | **REPORT IT** - Keep the baseline in the results table, on identical folds, every time the score is quoted. If the margin was flagged as small, say so. |
| INSUFFICIENT DATA | We could not check this - your file did not include a split column saying which rows were used for training and which for testing. | **FIXABLE** - Supply at least one of age, sex, device or site per row - and every such variable the data has, because a model can learn any of them, and run again. Until then this check has no verdict, and the report says so; it must not be read as a pass. |

### The demographic in disguise  (`proxy_probe`)

*Question:* Is the model's skill really a demographic variable wearing a model's clothes?

| layer | text |
| --- | --- |
| what it means (plain) | A model can beat the plain average and still be adding nothing: if what it learned IS the subject's sex or site, then a baseline that already knows the sex or site gains nothing from it. This check asks how much of the model's advantage survives once the demographic is already on the table. |
| mechanism (plain) | Take the model's advantage over a plain average. Now give the baseline the demographic variable too and ask how much of that advantage is left. If most of it vanishes, the model was a proxy for the demographic. A second probe asks the model's own output to guess the demographic; if it can, the demographic is in there. This project's 'PPG + demographics' model, at 0.824 g/dL, sat inside the viable band and added 0.007 to demographics alone. |
| mechanism (technical) | For each submitted demographic: advantage over the population mean, versus the advantage remaining when the demographic is already in the model (stacked ridge on identical folds); the fraction explained is reported per variable and the worst is judged. A logistic probe recovers the demographic from a supplied representation; on the prediction vector alone it is reported but does not count as a finding, because a model legitimately given sex as an input encodes sex in its output. |
| provenance | Phase 4 Task 3 and Phase 4.5's sex probe on learned representations (43.6-52.3% against a 57.0% base rate: the deep PPG models did not become sex classifiers; the feature model with demographics did). |
| glossary terms | proxy, baseline, representation |

**Headlines by verdict, with representative measured values:**

| verdict | plain headline | what to do |
| --- | --- | --- |
| FAIL | Most of what the model adds over a plain average - about 98% - disappears once sex is already known, so the model is largely detecting sex. The model's own output also reveals sex better than chance (measured on the prediction vector, which is reported but is not a finding on its own). | **REPORT IT** - Report the model's error WITH the demographic already in the baseline, and state that the image or signal adds little beyond it. Removing the demographic from the inputs does not help: the model reads it from the data. This project's 'PPG + demographics' model scored inside the viable band at 0.824 g/dL and was a sex classifier; its image CNN 'beat demographics' until site was added to them. |
| PASS | At most 10% of the model's advantage is explained by any demographic variable supplied (age is the closest), so the model is not simply reading a demographic. | **REPORT IT** - Report the increment the model adds over the demographic baseline, not only its standalone score. |
| INSUFFICIENT DATA | We could not check this - your file did not include a split column saying which rows were used for training and which for testing. | **FIXABLE** - Supply at least one demographic column - and, for a finding rather than a report, the model's internal representation as extra columns, and run again. Until then this check has no verdict, and the report says so; it must not be read as a pass. |

### Better than shuffled labels  (`permutation`)

*Question:* Is the model's score better than what pure chance would produce on this data?

| layer | text |
| --- | --- |
| what it means (plain) | Shuffle the true answers among the subjects and score the model again, many times. If the real score is not clearly better than the shuffled ones, the model has found nothing. If it is, the model has found something real - which is a low bar: real is not the same as useful. |
| mechanism (plain) | Every shuffle gives a score a model could get by luck. The p-value is the fraction of shuffles that did as well as the real model. Two traps: with n shuffles the smallest p-value you can ever see is 1/(n+1), so a p-value sitting exactly there is a bound, not a measurement; and if the model was picked as the best of several, the shuffles must re-run that choice, or the p-value flatters it. This project's one surviving positive result is p <= 0.0041 at 240 permutations - still the floor - and improves on predicting a constant by 0.05 g/dL. |
| mechanism (technical) | Labels are permuted across subjects against the FIXED submitted predictions; the empirical p is (draws at or below the real MAE + 1)/(n + 1), reported with the floor 1/(n+1) and a flag when it sits there; a parametric z is reported beside it and assumes a normal null. If y_pred__<name> candidate columns are supplied, the null re-selects the best candidate on each draw (selection-aware). This construction does not refit the model, so it is a different null from Phase 5's; verdicts, not p-values, are comparable across the two. |
| provenance | Phase 4.5 ceiling: feature model p = 0.978 (worse than shuffled). Phase 5: selection-aware p = 0.0164 at n = 60 -> 0.0041 at n = 240, at the floor both times; z -5.02 -> -4.96, the two statistics moving in opposite directions. |
| glossary terms | permutation test, p-value, floor, selection-aware, null |

**Headlines by verdict, with representative measured values:**

| verdict | plain headline | what to do |
| --- | --- | --- |
| FAIL | 489 of the 500 shuffles scored as well as or better than the real model (p = 0.98), so the model cannot be told apart from chance. | **STOP** - A model indistinguishable from shuffled labels has found nothing. Do not tune until it passes - a tuned pass against this null is the selection effect the check exists to catch. Report it. This project's hand-engineered PPG features permutation-tested at p = 0.978 and the representation was closed. |
| PASS | None of the 240 shuffles scored as well as the real model (p = 0.00415), so the model has learned something real. 0.00415 is the smallest p-value 240 permutations can produce; the true value may be smaller. It is a bound, not a measurement. The shuffles re-ran the choice among the candidate models, so picking the best of several is priced in. | **REPORT IT** - Report the empirical p beside any z-score, state the floor when the p sits on it, and say whether the null re-ran the model selection. Significance is not usefulness: this project's one surviving positive (p <= 0.0041 at n = 240) improves on a constant by 0.05 g/dL. |
| INSUFFICIENT DATA | We could not check this - your file did not include a split column saying which rows were used for training and which for testing. | **FIXABLE** - Run more permutations so the floor 1/(n+1) sits below the significance level, or supply what the check asked for. |

### Same model, different luck  (`seed_stability`)

*Question:* If you trained the same model again with a different random start, would the result hold?

| layer | text |
| --- | --- |
| what it means (plain) | Training has randomness in it. Two runs of the same code give two scores. If the improvement being claimed is no bigger than the wobble between runs, the claim is about a lucky run, not about the method. |
| mechanism (plain) | Compare the effect (how much better the model is than the null or the baseline) with the spread of scores across re-runs under different seeds. This project ran ten seeds and found a spread of 0.007 g/dL against an effect of 0.08 - eleven times larger - which is why the claim was kept. Its rule, declared before running, was to retract if the two were comparable. |
| mechanism (technical) | MAE per submitted y_pred_seed__<k> column; SD across seeds (ddof = 1) against the claimed effect - by default the model's advantage over the population mean on identical folds, or the real-vs-null gap when supplied. Fewer than three seeds gives a range, not a spread, and the check declines. |
| provenance | Phase 5 Task 1: 10 seeds, SD 0.0069, range 1.1069-1.1279, effect 0.0815, ratio 11.9. |
| glossary terms | seed, effect size |

**Headlines by verdict, with representative measured values:**

| verdict | plain headline | what to do |
| --- | --- | --- |
| FAIL | Re-running the same model with a different random seed moves its error by about as much as the effect being claimed (5 runs; spread 0.05 against an effect of 0.04), so the claimed improvement could be luck of the seed. | **STOP** - An effect no bigger than seed noise is not an effect. Report the seed spread beside the score and do not present the single best run. This project's rule, declared before running, was to retract if the seed spread was comparable to the real-vs-null gap. |
| PASS | The claimed effect (0.0815) is 12 times larger than the spread across 10 re-runs with different seeds (0.0069), so it is not an artefact of one lucky run. | **REPORT IT** - Report the number of seeds and the spread beside every score; quote the seed-averaged prediction, never the best seed. |
| INSUFFICIENT DATA | We could not check this - fewer than three re-runs of the model under different random seeds were supplied, so the spread between runs cannot be measured. | **FIXABLE** - Supply the same model retrained under at least three seeds, as y_pred_seed__<k> columns, and run again. Until then this check has no verdict, and the report says so; it must not be read as a pass. |

### Carried by a few subjects?  (`subgroup_robustness`)

*Question:* Does the result rest on a handful of people the model happens to do well on?

| layer | text |
| --- | --- |
| what it means (plain) | A model can look good on average because it is very good on a few subjects and ordinary on the rest. Screening has to work on the next person, so an advantage carried by a small group is not an advantage. |
| mechanism (plain) | Remove the tenth of subjects the model does best on and see how much of its advantage over a plain average survives. The trap is comparing the model before and after: removing easy subjects makes every model look worse, so the baseline has to be recomputed on the same reduced set. This project's surviving PPG result degraded from 1.11 to 1.23 g/dL after the drop but kept its advantage - a weak signal spread across the cohort, not a few lucky subjects. |
| mechanism (technical) | Per-subject absolute error; drop the best decile; recompute both the model's MAE and the population-mean baseline's MAE on the retained subjects; report the fraction of the original advantage retained. Per submitted subgroup (sex, site, device, age band), count subgroups where the model loses to the baseline. A retained fraction within 10 points of the threshold is flagged as near it. |
| provenance | Phase 5 Task 1: 1.1124 -> 1.2258 after dropping the best decile; the harness re-reads that as retained advantage with the baseline recomputed. |
| glossary terms | decile, baseline |

**Headlines by verdict, with representative measured values:**

| verdict | plain headline | what to do |
| --- | --- | --- |
| FAIL | Take away the 25 subjects the model does best on and only 41% of its advantage over a plain average remains, so the result rests on a small group of subjects. In 1 submitted subgroup the model loses to the baseline. The retained fraction is close to the threshold, so this verdict is not stable. | **REPORT IT** - Report per-subgroup error, and the error after dropping the best decile, beside the headline score. Do not remove the 'difficult' subjects to improve it - that is the failure in reverse. |
| PASS | Take away the 25 subjects the model does best on and 90% of its advantage over a plain average remains, so the result is spread across the cohort. | **REPORT IT** - Report the retained-advantage fraction and per-subgroup errors with the score, so the reader sees the effect is not carried by a few. |
| INSUFFICIENT DATA | We could not check this - your file did not include a split column saying which rows were used for training and which for testing. | **FIXABLE** - Supply held-out predictions for every subject (this check needs no extra column; it declined because there was no advantage to test), and run again. Until then this check has no verdict, and the report says so; it must not be read as a pass. |

### Is the signal there at all?  (`ceiling`)

*Question:* Do the inputs contain any information about the outcome, before any model is built?

| layer | text |
| --- | --- |
| what it means (plain) | If the measurements fed to a model carry no information about the target, no model can succeed and a failed model is not a modelling failure. This check looks at the inputs directly, without a model, and asks whether any of them relate to the outcome more than chance. |
| mechanism (plain) | For each input feature, measure how much it tells you about the outcome, then measure the same thing with the outcome shuffled. A feature that does no better than the shuffle is empty. This project's 51 hand-engineered PPG features were all below the shuffled null; the representation was closed - and then the raw waveform, tested separately, turned out to carry a small real signal the features had thrown away. |
| mechanism (technical) | Mutual information per feature against the target, compared with the 95th percentile of MI under a shuffled target; Pearson and Spearman correlations with Benjamini-Hochberg FDR at q. The verdict is the count of features above the MI null and the count surviving FDR. It is a necessary-condition test on the representation, not on any model. |
| provenance | Phase 4.5 ceiling analysis: 0 of 51 features above the MI null (max 0.061 vs null p95 0.068); 4 raw p < 0.05 against 2.6 expected; 0 survive FDR. |
| glossary terms | mutual information, FDR, null |

**Headlines by verdict, with representative measured values:**

| verdict | plain headline | what to do |
| --- | --- | --- |
| FAIL | None of the 51 input features carries more information about the outcome than a shuffled outcome would, so the input has no signal for any model to find. | **STOP** - When the inputs carry no information about the target, no model built on them can; a better model is not the remedy. Report that the representation is empty. This project found 0 of 51 PPG features above the null and stopped building feature models - and then found, separately, that the raw waveform did carry a small real signal. |
| PASS | 4 of the 12 input features carry more information about the outcome than shuffling would produce, and 3 correlations survive correction for multiple testing, so the input could contain the target. | **REPORT IT** - A non-empty input is a necessary condition, not a result. Report the number of features above the null and the multiple-testing correction. |
| INSUFFICIENT DATA | We could not check this - your file did not include a split column saying which rows were used for training and which for testing. | **FIXABLE** - Supply the model's input features as extra numeric columns, and run again. Until then this check has no verdict, and the report says so; it must not be read as a pass. |

## 2. Real outputs on the shipped sample submissions

The harness run on the synthetic samples in `app/backend/sample_data/`. Each row pairs the technical headline the check produced with the plain headline rendered from the same measured values.

### `01_mixed_start_here.csv`

| check | verdict | technical headline | plain headline |
| --- | --- | --- | --- |
| split_integrity | FAIL | 5 subject_ids appear in more than one split | 5 subjects have data in more than one part of the split, so the model was trained and tested on the same people. |
| demographic_baseline | PASS | model MAE 0.7359 beats the best single demographic (sex alone, 0.7406) by 0.0047 | The model beats the best single demographic guess (sex alone, 0.74 g/dL) with an error of 0.74 g/dL. The margin is small (+0.005 g/dL) compared with how much the outcome varies, so this verdict could flip with a different sample. |
| proxy_probe | PASS | at most 17% of the model's advantage is explained by any submitted demographic (sex) | At most 17% of the model's advantage is explained by any demographic variable supplied (sex is the closest), so the model is not simply reading a demographic. |
| permutation | PASS | empirical p = 0.00498 over 200 selection-aware permutations - AT THE FLOOR 1/(n+1)=0.00498, a bound, not a measurement | None of the 200 shuffles scored as well as the real model (p = 0.00498), so the model has learned something real. 0.00498 is the smallest p-value 200 permutations can produce; the true value may be smaller. It is a bound, not a measurement. The shuffles re-ran the choice among the candidate models, so picking the best of several is priced in. |
| seed_stability | PASS | effect is 335.5x the seed SD (0.1632 against 0.00049 over 5 seeds) | The claimed effect (0.163) is 336 times larger than the spread across 5 re-runs with different seeds (0.00049), so it is not an artefact of one lucky run. |
| subgroup_robustness | PASS | 57% of the model's advantage survives dropping its best 24 subjects | Take away the 24 subjects the model does best on and 57% of its advantage over a plain average remains, so the result is spread across the cohort. The retained fraction is close to the threshold, so this verdict is not stable. |
| ceiling | PASS | 1 of 2 features exceed the shuffled-target MI null, 1 survive FDR at q=0.05 | 1 of the 2 input features carries more information about the outcome than shuffling would produce, and 2 correlations survive correction for multiple testing, so the input could contain the target. |

### `02_clean_no_injected_fault.csv`

| check | verdict | technical headline | plain headline |
| --- | --- | --- | --- |
| split_integrity | INSUFFICIENT DATA | no subject_id spans a split across 240 subjects, but the content axis could not be checked | We could not check this - your file did not include the images themselves (an image folder, or an image_path column pointing at them). |
| demographic_baseline | PASS | model MAE 0.7913 beats the best single demographic (sex alone, 0.8157) by 0.0244 | The model beats the best single demographic guess (sex alone, 0.82 g/dL) with an error of 0.79 g/dL. The margin is small (+0.024 g/dL) compared with how much the outcome varies, so this verdict could flip with a different sample. |
| proxy_probe | PASS | at most 6% of the model's advantage is explained by any submitted demographic (sex) | At most 6% of the model's advantage is explained by any demographic variable supplied (sex is the closest), so the model is not simply reading a demographic. |
| permutation | PASS | empirical p = 0.00498 over 200 selected-model-only permutations - AT THE FLOOR 1/(n+1)=0.00498, a bound, not a measurement | None of the 200 shuffles scored as well as the real model (p = 0.00498), so the model has learned something real. 0.00498 is the smallest p-value 200 permutations can produce; the true value may be smaller. It is a bound, not a measurement. Only the submitted model was tested against the shuffles; if it was chosen as the best of several, that choice is not priced in. |
| seed_stability | PASS | effect is 196.0x the seed SD (0.1646 against 0.00084 over 5 seeds) | The claimed effect (0.165) is 196 times larger than the spread across 5 re-runs with different seeds (0.00084), so it is not an artefact of one lucky run. |
| subgroup_robustness | PASS | 51% of the model's advantage survives dropping its best 24 subjects | Take away the 24 subjects the model does best on and 51% of its advantage over a plain average remains, so the result is spread across the cohort. The retained fraction is close to the threshold, so this verdict is not stable. |
| ceiling | PASS | 1 of 2 features exceed the shuffled-target MI null, 1 survive FDR at q=0.05 | 1 of the 2 input features carries more information about the outcome than shuffling would produce, and 2 correlations survive correction for multiple testing, so the input could contain the target. |

### `03_fault_duplicates_across_split.csv`

| check | verdict | technical headline | plain headline |
| --- | --- | --- | --- |
| split_integrity | INSUFFICIENT DATA | no subject_id spans a split across 243 subjects, but the content axis could not be checked | We could not check this - your file did not include the images themselves (an image folder, or an image_path column pointing at them). |
| demographic_baseline | PASS | model MAE 0.7472 beats the best single demographic (sex alone, 0.8077) by 0.0605 | The model beats the best single demographic guess (sex alone, 0.81 g/dL) with an error of 0.75 g/dL. The margin is small (+0.060 g/dL) compared with how much the outcome varies, so this verdict could flip with a different sample. |
| proxy_probe | PASS | at most 48% of the model's advantage is explained by any submitted demographic (sex) | At most 48% of the model's advantage is explained by any demographic variable supplied (sex is the closest), so the model is not simply reading a demographic. |
| permutation | PASS | empirical p = 0.00498 over 200 selected-model-only permutations - AT THE FLOOR 1/(n+1)=0.00498, a bound, not a measurement | None of the 200 shuffles scored as well as the real model (p = 0.00498), so the model has learned something real. 0.00498 is the smallest p-value 200 permutations can produce; the true value may be smaller. It is a bound, not a measurement. Only the submitted model was tested against the shuffles; if it was chosen as the best of several, that choice is not priced in. |
| seed_stability | PASS | effect is 469.9x the seed SD (0.2776 against 0.00059 over 5 seeds) | The claimed effect (0.278) is 470 times larger than the spread across 5 re-runs with different seeds (0.00059), so it is not an artefact of one lucky run. |
| subgroup_robustness | PASS | 91% of the model's advantage survives dropping its best 24 subjects | Take away the 24 subjects the model does best on and 91% of its advantage over a plain average remains, so the result is spread across the cohort. |
| ceiling | PASS | 1 of 2 features exceed the shuffled-target MI null, 1 survive FDR at q=0.05 | 1 of the 2 input features carries more information about the outcome than shuffling would produce, and 2 correlations survive correction for multiple testing, so the input could contain the target. |

### `04_fault_subject_in_train_and_test.csv`

| check | verdict | technical headline | plain headline |
| --- | --- | --- | --- |
| split_integrity | FAIL | 5 subject_ids appear in more than one split | 5 subjects have data in more than one part of the split, so the model was trained and tested on the same people. |
| demographic_baseline | PASS | model MAE 0.6873 beats the best single demographic (sex alone, 0.8013) by 0.1140 | The model beats the best single demographic guess (sex alone, 0.80 g/dL) with an error of 0.69 g/dL. |
| proxy_probe | PASS | at most 0% of the model's advantage is explained by any submitted demographic (age) | At most 0% of the model's advantage is explained by any demographic variable supplied (age is the closest), so the model is not simply reading a demographic. |
| permutation | PASS | empirical p = 0.00498 over 200 selected-model-only permutations - AT THE FLOOR 1/(n+1)=0.00498, a bound, not a measurement | None of the 200 shuffles scored as well as the real model (p = 0.00498), so the model has learned something real. 0.00498 is the smallest p-value 200 permutations can produce; the true value may be smaller. It is a bound, not a measurement. Only the submitted model was tested against the shuffles; if it was chosen as the best of several, that choice is not priced in. |
| seed_stability | PASS | effect is 454.1x the seed SD (0.1868 against 0.00041 over 5 seeds) | The claimed effect (0.187) is 454 times larger than the spread across 5 re-runs with different seeds (0.00041), so it is not an artefact of one lucky run. |
| subgroup_robustness | PASS | 64% of the model's advantage survives dropping its best 24 subjects | Take away the 24 subjects the model does best on and 64% of its advantage over a plain average remains, so the result is spread across the cohort. The retained fraction is close to the threshold, so this verdict is not stable. |
| ceiling | PASS | 2 of 2 features exceed the shuffled-target MI null, 1 survive FDR at q=0.05 | 2 of the 2 input features carry more information about the outcome than shuffling would produce, and 2 correlations survive correction for multiple testing, so the input could contain the target. |

## 3. The three what-to-do categories

* **FIXABLE** - A concrete methodological correction exists. It changes how the evidence is produced, not the model, and it can make the number worse - that is the point.
* **REPORT IT** - Nothing in the code fixes this. The honest response is to state the limitation next to the result, every time the result appears.
* **STOP** - The result says the approach does not reach the accuracy the claim needs. The correct action is to report the negative result, not to search for a variant that passes.

## 4. Glossary

* **baseline** - The simplest competitor: predicting the average, or predicting from one obvious variable such as sex. A model that cannot beat it on the same folds is not measuring what it claims.
* **connected components** - Groups formed by following links: if record A shares a person with B and B shares a picture with C, all three belong together and must land on the same side of the split.
* **content hash** - A fingerprint computed from an image's pixels. Two files with the same hash are the same picture, whatever they are called.
* **decile** - A tenth of the subjects. Dropping the best decile removes the 10% the model does best on.
* **effect size** - How much better the model is than the null or the baseline, in the target's units. It has to be compared with the noise around it to mean anything.
* **FDR** - False discovery rate: when many correlations are tested at once, some look significant by chance. The correction keeps the expected fraction of false discoveries below a set level.
* **floor** - With n shuffles the smallest p-value that can appear is 1/(n+1). A p-value sitting exactly there is a bound - the true value may be smaller - not a measurement.
* **g/dL** - Grams per decilitre, the unit haemoglobin is measured in. Normal adult values are roughly 12-17.
* **identical folds** - The data was divided into the same groups for the model and for every baseline, so the comparison is fair.
* **INSUFFICIENT DATA** - The check could not run because the file lacked what it needs. It is not a pass and not a fail; nothing was inferred from what could not be measured.
* **leakage** - Information from the test set reaching the model during training - a shared person, a duplicated image, a decision tuned against the test score. It inflates the score without improving the model.
* **MAE** - Mean absolute error: on average, how far the model's prediction is from the true value, in the target's own units (here grams of haemoglobin per decilitre of blood). Smaller is better.
* **mutual information** - How much knowing one quantity reduces uncertainty about another. Zero means no relationship of any kind, linear or not.
* **near-duplicate** - Two images that are not byte-identical but are the same picture - re-saved, resized or slightly cropped. Perceptual hashes catch these.
* **null** - What the statistic looks like when there is nothing to find: the distribution of scores under shuffled labels.
* **p-value** - The fraction of shuffles that scored as well as the real model. Small means the real score is hard to get by luck.
* **permutation test** - Shuffle the true answers among the subjects many times and score the model each time. The result shows what scores luck alone produces.
* **pre-registration** - Writing down the thresholds a result will be judged against before seeing the result. It is what makes a verdict a verdict rather than an interpretation.
* **proxy** - Something the model has learned in place of the thing it claims to measure - sex instead of haemoglobin, for instance.
* **representation** - The internal numbers a model computes from its input before producing a prediction. Probing them shows what the model has actually encoded.
* **seed** - The random starting point of a training run. Different seeds give different results from the same code; the spread between them is noise, not signal.
* **selection-aware** - When a model was chosen as the best of several, the shuffled runs re-do that choice, so the p-value accounts for having picked the winner.
* **split** - How the data was divided into the part used to train the model and the part used to test it. The test part must contain people the model never saw.
* **WHO band** - The World Health Organization's haemoglobin thresholds: anaemia below 12 g/dL in women and 13 in men; severity bands at 7, 10 and 11 g/dL.

## 5. Review notes

* INSUFFICIENT DATA: every plain headline begins "We could not check this" and names the missing input; the what-to-do text says it must not be read as a pass. Enforced by `tests/test_phase8a.py`.
* Permutation at the floor: the plain headline states the floor in words and that the true value may be smaller; whether the null re-ran the model selection is stated either way. Enforced by test.
* Narrow margins (demographic baseline) and near-threshold retained fractions (subgroup robustness) are carried into the plain headline. Enforced by test.
* No what-to-do text is phrased as a way to make a failing check pass; FAIL is FIXABLE only for the two split-construction checks. Enforced by test.
* Known limit: plain headlines are templates over measured values; a check that gains a new measured key or a new caveat must extend the template, and the test's MEASURED table, at the same time.
