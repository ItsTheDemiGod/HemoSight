# HemoSight Web (Phase 9 — not started)

Placeholder. Planned stack:

- **Backend:** FastAPI, SQLite for development, PostgreSQL-ready via SQLAlchemy.
- **Frontend:** React + TypeScript + Vite, Tailwind CSS, Framer Motion,
  Three.js / react-three-fiber for 3D.

Every result surface must present a three-state output (screen negative /
screen positive-refer / cannot decide), show the uncertainty interval, and carry
the screening-not-diagnosis framing. See CLAUDE.md, Phase 9.
